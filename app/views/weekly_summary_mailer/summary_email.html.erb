<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8" />
  <title>Weekly Habit Summary</title>
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
      background-color: #f9f9f9;
      margin: 0; padding: 0;
      color: #333;
    }
    .container {
      max-width: 600px;
      margin: 30px auto;
      background-color: #ffffff;
      border-radius: 8px;
      padding: 30px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    }
    h1 {
      color: #2c3e50;
      font-weight: 700;
      font-size: 24px;
      margin-bottom: 20px;
      border-bottom: 2px solid #3498db;
      padding-bottom: 10px;
    }
    h3 {
      color: #34495e;
      margin-top: 20px;
      margin-bottom: 8px;
      font-weight: 600;
    }
    p {
      margin: 4px 0;
      font-size: 15px;
      line-height: 1.5;
      color: #555;
    }
    .habit-summary {
      border-top: 1px solid #e1e4e8;
      padding-top: 15px;
    }
    .footer {
      margin-top: 40px;
      font-size: 13px;
      color: #999;
      text-align: center;
    }
    @media only screen and (max-width: 620px) {
      .container {
        width: 90% !important;
        padding: 20px !important;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Hello <%= @user.first_name.presence || @user.email %>, here’s your weekly habit summary!</h1>

    <% if @habits.any? %>
      <% @habits.each do |habit| %>
        <div class="habit-summary">
          <h3><%= habit.name %></h3>
          <p><strong>Total Completions:</strong> <%= habit.habit_checkins.count %></p>
          <p><strong>Current Streak:</strong> <%= habit.current_streak %></p>
          <p><strong>Longest Streak:</strong> <%= habit.longest_streak %></p>
          <p><strong>Consistency:</strong> <%= habit.consistency_percentage %> %</p>
        </div>
      <% end %>
    <% else %>
      <p>You don’t have any habits tracked this week. Start tracking today to see your progress!</p>
    <% end %>

    <div class="footer">
      <p>Thank you for using Habit Tracker – “Consistently”.</p>
      <p>If you have questions, reply to this email or visit our website.</p>
    </div>
  </div>
</body>
</html>
